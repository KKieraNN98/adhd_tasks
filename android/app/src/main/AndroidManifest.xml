<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ===== Permissions ===== -->

    <!-- Android 13+ (API 33): runtime permission for posting notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Exact alarms: safe to include when you intend to request special access on Android 12+. -->
    <!-- This is compatible with using AndroidScheduleMode.alarmClock (which doesn't require it),
         and with your best-effort request flow. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Receive reboot so scheduled alarms can be re-registered -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Commonly used with alarm scheduling to ensure wake -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Optional; some OEMs/reference paths still check this -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />


    <application
        android:label="adhd_todo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below. Used by Flutter tooling. -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ===== flutter_local_notifications explicit receivers (Android 12+ requires exported=true) ===== -->
        <!-- Receives the scheduled AlarmManager broadcasts and posts the notification -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="true" />

        <!-- Handles BOOT_COMPLETED (and package replace) to recover scheduled notifications -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- NOTE:
             These <receiver> entries are normally merged in by the plugin, but on some projects
             (especially with Android 12/13+) the merge can leave them non-exported or missing.
             Declaring them explicitly guarantees AlarmManager deliveries reach your app. -->
    </application>

    <!-- Package visibility (kept from your original) -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
